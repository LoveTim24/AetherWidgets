name: AetherWidgets
options:
  bundleIdPrefix: com.revo
targets:
  AetherWidgets:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources: [AetherWidgets]
    settings:
      PRODUCT_BUNDLE_IDENTIFIER: com.revo.aetherwidgets
